import { CameraFeed } from '../components/CameraFeed'; import { cameras } from '../data/demo';
export function Monitoring(){return <><section className="page-intro"><div><div className="eyebrow">LIVE VIDEO SURVEILLANCE</div><h1>Multi-Camera Monitor</h1><p>All feeds are simulated in DEMO MODE. Connect RTSP cameras whenever you are ready.</p></div><div className="grid-switch"><button className="selected">2 × 2</button><button>3 × 3</button><button>4 × 4</button></div></section><section className="camera-grid monitor-grid">{cameras.map(c=><CameraFeed key={c.id} camera={c}/>)}</section></>}
