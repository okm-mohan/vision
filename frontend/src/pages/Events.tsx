import { Search, SlidersHorizontal } from 'lucide-react';import { events } from '../data/demo';
export function Events(){return <><section className="page-intro"><div><div className="eyebrow">DETECTION HISTORY</div><h1>AI Events</h1><p>Review, acknowledge, and investigate every meaningful detection.</p></div></section><div className="filters"><div><Search size={17}/><input placeholder="Search event ID, camera, object…"/></div><button><SlidersHorizontal size={16}/> Filters</button><button>Today ▾</button></div><section className="panel table"><div className="table-head"><span>EVENT</span><span>CAMERA / LOCATION</span><span>OBJECT</span><span>TIME</span><span>SEVERITY</span><span>STATUS</span><span/></div>{events.concat(events).map((e,i)=><div className="table-row" key={i}><div><b>{e.type}</b><small>{e.id.slice(4)}</small></div><div><b>{e.camera}</b><small>ManPro network</small></div><span>{e.object}</span><span>{e.time}</span><span className={`pill ${e.severity}`}>{e.severity}</span><span className="pill normal">New</span><button className="mini">View</button></div>)}</section></>}
